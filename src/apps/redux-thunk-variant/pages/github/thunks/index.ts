import { incrementReaction } from './incrementReaction';
import { initCards } from './initCards';
import { positionChange } from './positionChange';
import { setDragging } from './setDragging';

export const githubThunks = {
  incrementReaction,
  initCards,
  positionChange,
  setDragging
};
